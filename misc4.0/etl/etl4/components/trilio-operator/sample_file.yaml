run:
  namespace: default
  storageClass: default
  cleanupOnFailure: true
  pvcStorageRequest: 1Gi
  resources:
    requests:
      memory: 64Mi
      cpu: 250m
    limits:
      memory: 128Mi
      cpu: 500m

cleanup:
  namespace: default
  logLevel: info
