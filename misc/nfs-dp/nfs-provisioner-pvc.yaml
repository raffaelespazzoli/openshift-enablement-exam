kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: myclaim
  annotations:
    volume.beta.kubernetes.io/storage-class: "local-pod"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi